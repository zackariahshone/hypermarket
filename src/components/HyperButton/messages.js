/*
 * HyperButton Messages
 *
 * This contains all the text for the HyperButton component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.HyperButton';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HyperButton component!',
  },
});
